# Cloudflare Runtime Modules

Place isolated Durable Object, Queue consumer, Workflow, or specialized Worker modules here only when the React Router Worker cannot responsibly own the behavior. Keep boundaries tied to a real operational need.
