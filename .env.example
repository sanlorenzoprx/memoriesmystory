# Copy to .env.local for a Clerk/Stripe-enabled local Vite build. Never commit .env.local.
# These keys are designed to be public, but remain environment-specific.
VITE_CLERK_PUBLISHABLE_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=
